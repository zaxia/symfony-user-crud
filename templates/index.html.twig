<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Accueil</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
        <link rel="stylesheet" href="{{ asset('lib/css/bootstrap.min.css') }}" type="text/css" />
        {{ include('layouts/styles.html.twig') }}
    </head>
    <body>
        {{ include('layouts/header.html.twig') }}
        <div class="container">
            <div class="jumbotron">
            <h1 class="display-4">Bienvenue</h1>
            </div>
        </div>
        <div class="container"><button id="calcul_test" class="btn btn-info">Faire un test de calcul</button></div>
        <div id="chat">
            <div id="messages">
            </div>
            <div id="message">
                <div class="form-inline"><input placeholder="pseudo" id="chat-pseudo" value="{{ user.getLogin() }}" readonly/><input placeholder="message..." id="chat-message"/></div>
            </div>
        </div>
{{ include('layouts/footer.html.twig') }}